package observatory.observations.common.util;

import net.minecraft.entity.Entity;

public interface AttributeEntityValue {

    Entity observations$getEntity();
    void observations$setEntity(Entity entity);
}
